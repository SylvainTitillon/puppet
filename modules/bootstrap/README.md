Classe de bootstrapping :
bootstrap : fait venir une conf un peu bourrine sshd et un utilisateur système
bootstrap::puppet : garantit la présence de puppet-agent (et du dépot) pour les RH like, et Debian
